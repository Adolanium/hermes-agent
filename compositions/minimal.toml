# CLI-only programming agent. No TUI, desktop, or gateway.
[plugins]
enabled = [
  "hermes.persistence.sqlite",
  "hermes.tools.core",
  "hermes.policy.workspace",
  "hermes.tools.fs",
  "hermes.tools.terminal",
  "hermes.provider.openai-compat",
  "hermes.agent.runtime",
  "hermes.interface.stdio",
]

[services]
# Required because the unique model.provider slot is explicit.
"model.provider" = "hermes.provider.openai-compat"

[plugins.settings."hermes.provider.openai-compat"]
base_url = "http://127.0.0.1:0/v1"
api_key = ""
model = "local-fixture"
